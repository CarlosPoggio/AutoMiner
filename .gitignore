# Tu configuración real (contiene tu wallet) nunca se sube a git
config.md

# Motores de minado: la propia app los descarga solos en bin/ (o los
# coloca aquí el usuario a mano). Pesan mucho y son distintos por SO,
# así que no se suben a git.
bin/*
!bin/LEEME.md

# Python
__pycache__/
*.pyc
.pytest_cache/
